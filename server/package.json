{
  "name": "server",
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "start": "node index.js",
    "build": "npx prisma generate",
    "postbuild": "npx prisma migrate deploy",
    "prisma": "prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.x",
    "bcrypt": "^5.x",
    "cors": "^2.x",
    "dotenv": "^16.x",
    "express": "^4.x",
    "jsonwebtoken": "^9.x"
  },
  "devDependencies": {
    "prisma": "^5.x",
    "nodemon": "^3.x"
  }
}
